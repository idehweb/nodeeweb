
import model from './model.mjs'
import routes from './routes.mjs'

export default {
    "name": "element",
    "model": model,
    "modelName": "Element",
    "routes": routes,

    "views": [{
        "func": (req, res, next) => {
        }
    }],
    "edits": [{
        "func": (req, res, next) => {
        }
    }],

}